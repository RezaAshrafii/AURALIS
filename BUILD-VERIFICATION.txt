AURALIS v0.12.0 — BUILD VERIFICATION
====================================

Milestone:
  Production Audio Core

Baseline:
  v0.11.2 Runtime/Auth Stabilization

Implemented in source:
  - Rust auralis-core v0.12.0
  - direct Windows WASAPI capture contracts
  - microphone + system loopback capture paths
  - event-driven capture contract
  - bounded capture handoff queue
  - dropped-buffer / dropped-sample accounting
  - append-only raw spool
  - SHA-256 + sequence range integrity
  - SQLite WAL audio ledger
  - lifecycle / device interruption persistence
  - crash/recovery scan and resume cursors
  - real-Windows mic/loopback/both hardware-gate runner
  - capture-summary verifier
  - quick 60/60/120 second gate launcher
  - 20-minute simultaneous-capture gate launcher

Preserved deliberately:
  - existing focused UI and interaction model
  - current validated live transcript bridge
  - Turn isolation / ownership
  - Auto Answer + Hotkey Z
  - RAG / citation validation
  - Gemini credential preflight and actionable auth failures

Important integration rule:
  The Rust v0.12 hardware runner is NOT silently promoted into the interactive
  Live Transcript path. v0.13 must add the production Neural-VAD / Streaming-ASR
  event bridge before that migration. This prevents a native-core milestone from
  regressing the working transcript/answer flow.

Executed in this environment:
  Source Node regression suite:
    tests   85
    pass    74
    fail    0
    skipped 11

  Portable Node regression suite:
    tests   85
    pass    75
    fail    0
    skipped 10

  npm run verify:             PASS (AURALIS_VERIFY_PASS)
  frontend TypeScript check: PASS
  frontend deterministic build: PASS
  server.mjs syntax:         PASS
  app-react.js syntax:       PASS
  provider-errors.mjs syntax: PASS

  Source <-> Portable common-file comparison:
    shared files: 94
    mismatches:   0

  Windows binaries already shipped in Portable:
    runtime/bun.exe                  PE32+ x86-64
    native/auralis-capture-probe.exe PE32+ x86-64
    native/auralis-spool-inspect.exe PE32+ x86-64

NOT executed / NOT claimed:
  - cargo fmt / clippy / cargo test
  - Rust Windows release build
  - real microphone capture through Rust core
  - real system-loopback capture through Rust core
  - 20-minute Windows hardware gate
  - 120-minute release soak

Reason:
  This build environment has no rustc/cargo and is not a real Windows audio host.

Required Windows validation:
  1) BUILD-V012-RUST-CORE.cmd
  2) RUN-V012-QUICK-HARDWARE-GATE.cmd
  3) RUN-V012-20MIN-HARDWARE-GATE.cmd

Promotion criterion:
  v0.12 hardware PASS may only be claimed after the real-Windows summaries show
  durable sequences > 0, dropped samples/buffers = 0 and unknown gaps = 0 for the
  required uninterrupted acceptance runs.
